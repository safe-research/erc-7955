{
  "name": "@erc-7955/monorepo",
  "private": true,
  "description": "ERC-7955: Permissionless CREATE2 Factory",
  "author": "Safe Research <research@safe.global>",
  "license": "GPL-3.0-only",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run compile -w packages/core && tsc --build && npm run build -w packages/app",
    "lint": "biome check",
    "start": "npm start -w packages/app --",
    "test": "npm test -w packages/core --"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.2",
    "typescript": "^5.9.2"
  }
}
